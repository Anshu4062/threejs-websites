import { SourceLocation, BundleSize } from '@million/shared/types';

interface Import {
    specifiers: Array<string>;
    source: string;
    start: number;
    end: number;
    loc: SourceLocation;
}

interface WorkerProps {
    source: string;
    filename: string | undefined;
    imports: Array<Import>;
}
declare const _default: ({ source, filename, imports }: WorkerProps) => Promise<BundleSize[]>;

export = _default;
